import 'package:flutter/material.dart';

class FoucesScreen extends StatelessWidget {
  const FoucesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child:Text('Fouces',style: TextStyle(color: Colors.white),)
      ),
    );
  }
}
